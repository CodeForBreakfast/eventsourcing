/**
 * WebSocket Event Transport
 *
 * A domain-agnostic transport layer for streaming events over WebSocket connections.
 * Integrates with the eventstore infrastructure to provide real-time event streaming
 * and command handling capabilities.
 */

export * from './types';
export * from './interface';
export * from './implementation';
